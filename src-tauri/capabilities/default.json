{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default capability set for the AttyMate webview. Keep this narrow — only enable Tauri APIs we actually call from the SPA.",
  "windows": ["main"],
  "permissions": [
    "core:default"
  ]
}
